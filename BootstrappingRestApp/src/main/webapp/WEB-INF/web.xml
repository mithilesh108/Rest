<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
	<display-name>Archetype Created Web Application</display-name>
	<servlet>
		<servlet-name>jersy</servlet-name>
		<servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
		<!--#1 will scan all sub packages by default if scanning.recuive property dont configures -->
		<init-param>
			<param-name>jersey.config.server.provider.packages</param-name>
			<param-value>com.tcs</param-value>
		</init-param>
		
		<!--#2 false menas will stop to scane looking in sub packages 
	       default value = true menas will scane all sub packages based on package configurations-->
		<init-param> 
			<param-name>jersey.config.server.provider.scanning.recursive</param-name>
			<param-value>true</param-value>
		</init-param>
		
		<load-on-startup>1</load-on-startup>
	</servlet>

	<servlet-mapping>
		<servlet-name>jersy</servlet-name>
		<url-pattern>/api/*</url-pattern>
	</servlet-mapping>

</web-app>
